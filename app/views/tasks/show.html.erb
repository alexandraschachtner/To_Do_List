<h1> Task Details </h1>
<p>
  <strong>Who:</strong>
  <%= @task.who %>
</p>

<p>
  <strong>Name:</strong>
  <%= @task.name %>
</p>

<p>
  <strong>Details:</strong>
  <%= @task.details %>
</p>

<p>
<%= link_to 'See all Tasks', tasks_path %></br>
<%= link_to 'Edit this Task', edit_task_path(@task) %> </br>
<%= link_to 'This task is finished. (delete)', task_path(@task),
        method: :delete,
        data: { confirm: 'Are you sure? More importantly, did you ask Mom?' } %>
 </p>
